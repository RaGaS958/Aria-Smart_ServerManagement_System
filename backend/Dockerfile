# ─────────────────────────────────────────────────────────────────
#  ARIA System — Dockerfile  (lives inside backend/)
#  Deploy target : Hugging Face Spaces (Docker SDK)
#  Env name      : ariavenv
#  Port          : 7860  ← HF Spaces required port
# ─────────────────────────────────────────────────────────────────

# ── Stage 1: base ─────────────────────────────────────────────────
FROM python:3.11-slim AS base

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    VIRTUAL_ENV=/app/ariavenv \
    PATH="/app/ariavenv/bin:$PATH" \
    PORT=7860

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
        curl \
    && rm -rf /var/lib/apt/lists/*

# ── Stage 2: install dependencies ─────────────────────────────────
FROM base AS deps

RUN python -m venv /app/ariavenv

# Dockerfile context = backend/ so no path prefix needed
COPY requirements.txt ./

RUN pip install --upgrade pip --quiet \
 && pip install --no-cache-dir -r requirements.txt

# ── Stage 3: production image ──────────────────────────────────────
FROM base AS production

COPY --from=deps /app/ariavenv /app/ariavenv

# Copy backend source — tests/, htmlcov/, ariavenv/ are excluded
# by .dockerignore so they never reach the image
COPY . .

# HF Spaces runs containers as root (non-root breaks HF file perms)
EXPOSE 7860

HEALTHCHECK --interval=30s --timeout=10s --start-period=20s --retries=3 \
    CMD curl -f http://localhost:7860/health || exit 1

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "7860"]